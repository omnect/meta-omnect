#!/bin/sh
#
# Purpose of this script is to record service terminations, regular as well as
# unintentional ones, in a generic way so that such occurrences can be examined
# by a health check routine.
#

ME="${0##*/}"
