PREFERRED_PROVIDER_virtual/kernel ?= "linux-welotec"

# initramfs type
OMNECT_INITRAMFS_FSTYPE = "cpio.gz"

# unset welotec's defaults
INITRAMFS_IMAGE = ""
INITRAMFS_IMAGE_BUNDLE = ""
INITRAMFS_MAXSIZE = ""

# @todo remove me when solved where ee-acpi-scripts is located
#MACHINE_EXTRA_RRECOMMENDS:remove = "eee-acpi-scripts"

# @todo why do we have to explicitly unset this here?
# even if swupdates defconfig doesn't include "CONFIG_UBOOT=y" it
# depends on libubootenv until we clear PREFERRED_PROVIDER_u-boot-fw-utils
PREFERRED_PROVIDER_u-boot-fw-utils = ""
