part U-BOOT --no-table --offset ${BOOTLOADER_SEEK} --source rawcopy --sourceparams="file=bootloader.versioned.bin"
part --no-table --offset ${OMNECT_PART_OFFSET_UBOOT_ENV1} --source rawcopy --sourceparams="file=uboot-env.bin"
part --no-table --offset ${OMNECT_PART_OFFSET_UBOOT_ENV2} --source rawcopy --sourceparams="file=uboot-env.bin"
part --offset ${OMNECT_PART_OFFSET_BOOT} --fixed-size ${OMNECT_PART_SIZE_BOOT}K --fstype=vfat --label boot --source bootimg-partition --active

include omnect-os.common.wks.inc

bootloader --ptable gpt
