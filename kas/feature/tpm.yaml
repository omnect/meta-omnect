# kas config file

header:
  version: 11

local_conf_header:
  meta-omnect_feature_tpm: |
    DISTRO_FEATURES:append = " tpm2"
    MACHINE_FEATURES:append = " tpm2"

    EXTRA_OECONF:append:pn-tpm2-tss = " --disable-weakcrypto --enable-debug=info"
    OMNECT_DEVEL_TOOLS_DEFAULT:append = " tpm2-tools"
