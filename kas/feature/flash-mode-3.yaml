# kas config file

header:
  version: 14

local_conf_header:
  meta-omnect_feature_flash-mode-3: |
    DISTRO_FEATURES:append = " flash-mode-3"

    OMNECT_UBOOT_WRITEABLE_ENV_FLAGS:append = "flash-mode-url:sw,flash-mode-sha256:sw,"
