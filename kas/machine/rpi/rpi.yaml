# kas config file

header:
  version: 10

repos:
  ext/meta-raspberrypi:
    url: "https://github.com/agherzan/meta-raspberrypi.git"
    branch: "scarthgap"
    commit: "1918a27419dcd5e79954c0dc0edddcde91057a7e"
    patches:
      p001:
        repo: "meta-omnect"
        path: "kas/patches/meta-raspberrypi_layerdir.patch"

local_conf_header:
  meta-omnect_machine_rpi_rpi: |
    # TODO scarthgap: we should handle that via env var opt in
    LICENSE_FLAGS_ACCEPTED += "synaptics-killswitch"
